import styled from "@emotion/styled";

export const Wrapper = styled.div`
  position: relative;
  z-index: 1;
  height: 400px;
  width: 100%;
`;

export const SliderItem = styled.img`
  position: relative;
  z-index: 1;
  height: 350px;
  margin: auto;
`;
